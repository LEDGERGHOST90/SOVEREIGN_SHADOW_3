# Sovereign Shadow II - Production Requirements
# Trading system with NetworkChuck strategy enforcement

# Core Framework
python>=3.11

# Data Validation & Schemas
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Exchange APIs (CCXT Library)
ccxt>=4.1.0

# Ethereum & DeFi
web3>=6.0.0
eth-account>=0.9.0
eth-utils>=2.0.0

# HTTP Requests & APIs
requests>=2.31.0
aiohttp>=3.8.0
websockets>=11.0.0

# CLI & UI
rich>=13.0.0  # For beautiful terminal output
tabulate>=0.9.0  # For table formatting

# Configuration & Environment
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Logging & Monitoring
python-json-logger>=2.0.0
colorlog>=6.7.0

# Date & Time
python-dateutil>=2.8.2

# Cryptography
cryptography>=41.0.0

# JSON Processing
orjson>=3.9.0  # Fast JSON serialization (optional)

# Rate Limiting
ratelimit>=2.2.1

# API Documentation (Optional)
pydantic-openapi-schema>=1.0.0

# MCP Server (Claude Desktop Integration)
fastapi>=0.104.0  # For MCP server REST API
uvicorn>=0.24.0  # ASGI server

# Utilities
attrs>=23.1.0
dataclasses-json>=0.6.0

# Obsidian Integration (Optional)
markdown>=3.4.0

# Performance
numpy>=1.24.0  # For analytics calculations
pandas>=2.0.0  # For trade journal analytics (optional)

# Production Dependencies
gunicorn>=21.0.0  # Production WSGI server (if using web interface)

# Security
certifi>=2023.0.0  # SSL certificates

# API Keys Management
keyring>=24.0.0  # Secure credential storage (optional)

# Backup & Restore
python-gnupg>=0.5.0  # GPG encryption for backups (optional)

# === NOTES ===
#
# Development dependencies (testing, linting, type checking) are in requirements-dev.txt
# Install with: pip install -r requirements-dev.txt
#
# === INSTALLATION ===
#
# Production:
#   pip install -r requirements.txt
#
# Development:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
#
# Upgrade All:
#   pip install -r requirements.txt --upgrade
#
# === EXCHANGE-SPECIFIC NOTES ===
#
# CCXT: Unified API for 100+ exchanges (Coinbase, Binance US, OKX, Kraken)
# Web3.py: Ethereum blockchain interaction (AAVE v3 monitoring)
#
# === SYSTEM REQUIREMENTS ===
#
# Python: 3.11 or higher
# OS: macOS, Linux, Windows
# Memory: 2GB RAM minimum
# Storage: 1GB for logs and journal data
