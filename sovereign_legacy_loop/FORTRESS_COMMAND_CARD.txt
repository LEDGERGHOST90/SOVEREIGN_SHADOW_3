═══════════════════════════════════════════════════════════════════
🏴 SOVEREIGN SHADOW AI - FORTRESS REORGANIZATION COMMAND CARD 🏴
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: SCAN YOUR STRUCTURE                                     │
└─────────────────────────────────────────────────────────────────┘

cd /Volumes/LegacySafe/SovereignShadow.Ai[LegacyLoop]
./scan_sovereign_structure.sh

Output: ~/Desktop/SOVEREIGN_STRUCTURE_AUDIT_[timestamp].txt
→ Review the audit before proceeding


┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: BUILD FORTRESS FOUNDATION                               │
└─────────────────────────────────────────────────────────────────┘

cd /Volumes/LegacySafe/SovereignShadow.Ai[LegacyLoop]
./reorganize_sovereign_fortress.sh

Creates:
✓ Complete domain-driven directory structure
✓ Base classes (BaseEngine, BaseExchange)
✓ Example implementations
✓ Migration guide (MIGRATION_GUIDE.md)
✓ Suggested commands (MIGRATION_COMMANDS.sh)
✓ Full backup (reorganization_backup_[timestamp]/)


┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: SHARE AUDIT WITH CLAUDE                                 │
└─────────────────────────────────────────────────────────────────┘

1. Open audit file from Desktop
2. Copy ALL contents
3. Paste into Claude
4. Receive custom migration scripts for YOUR files


┌─────────────────────────────────────────────────────────────────┐
│ VALIDATION COMMANDS                                              │
└─────────────────────────────────────────────────────────────────┘

cd sovereign_legacy_loop

npm run type-check    # TypeScript compilation
npm run lint          # Code quality
npm test              # Run tests
npm run build         # Production build
npm run dev           # Start dev server


┌─────────────────────────────────────────────────────────────────┐
│ GIT SAFETY COMMANDS                                              │
└─────────────────────────────────────────────────────────────────┘

# Before starting
git add -A
git commit -m "Pre-reorganization checkpoint"
git checkout -b feature/fortress-reorganization

# After completion
git add -A
git commit -m "feat: fortress architecture reorganization"

# If something goes wrong
git checkout main
git branch -D feature/fortress-reorganization


┌─────────────────────────────────────────────────────────────────┐
│ QUICK FILE FINDER                                                │
└─────────────────────────────────────────────────────────────────┘

cd sovereign_legacy_loop

# Find trading engines
find lib/trading/engines -name "*.ts"

# Find exchange adapters
find lib/trading/exchanges -name "*.ts"

# Find components
find components -name "*.tsx"

# Find loose files in root
find . -maxdepth 1 -type f \( -name "*.ts" -o -name "*.tsx" \)


┌─────────────────────────────────────────────────────────────────┐
│ NEW STRUCTURE AT A GLANCE                                        │
└─────────────────────────────────────────────────────────────────┘

sovereign_legacy_loop/
├── app/              # Next.js routes (pages)
├── components/       # React UI components
│   ├── ui/          # Base components
│   ├── trading/     # Trading UI
│   ├── portfolio/   # Portfolio UI
│   └── dashboard/   # Dashboard UI
├── lib/             # Business logic (core)
│   ├── trading/     # Trading domain
│   │   ├── engines/     # Trading engines
│   │   ├── exchanges/   # Exchange adapters
│   │   ├── strategies/  # Trading strategies
│   │   └── risk/        # Risk management
│   ├── portfolio/   # Portfolio domain
│   ├── ai/          # AI agents domain
│   ├── blockchain/  # Blockchain domain
│   └── utils/       # Shared utilities
├── hooks/           # React hooks
├── types/           # TypeScript types
├── config/          # Configuration
└── scripts/         # Python backend


┌─────────────────────────────────────────────────────────────────┐
│ IMPORT PATTERN EXAMPLES                                          │
└─────────────────────────────────────────────────────────────────┘

// ✅ CORRECT: Absolute imports
import { SpotEngine } from '@/lib/trading/engines/spot/SpotEngine'
import { BaseExchange } from '@/lib/trading/exchanges/BaseExchange'
import { Button } from '@/components/ui/button'

// ❌ WRONG: Relative imports
import { SpotEngine } from '../../../lib/trading/engines/spot/SpotEngine'


┌─────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                  │
└─────────────────────────────────────────────────────────────────┘

"Cannot find module @/lib/..."
→ Check tsconfig.json paths configuration

"Circular dependency detected"
→ Move shared types to types/ directory

"Tests failing after migration"
→ Update test imports to match new structure

"TypeScript errors everywhere"
→ Run: npm run type-check | head -20
→ Fix errors one domain at a time


┌─────────────────────────────────────────────────────────────────┐
│ DOCUMENTATION FILES                                              │
└─────────────────────────────────────────────────────────────────┘

FORTRESS_QUICK_START.md          # This guide
SOVEREIGN_REORGANIZATION_PLAN.md # Full architecture
MIGRATION_GUIDE.md               # Detailed migration steps
MIGRATION_COMMANDS.sh            # Generated migration commands
lib/README.md                    # Core library documentation


┌─────────────────────────────────────────────────────────────────┐
│ SUPPORT                                                          │
└─────────────────────────────────────────────────────────────────┘

Need help?
1. Run the scanner to get current state
2. Copy output or error messages
3. Paste to Claude with context
4. Get instant custom solution

═══════════════════════════════════════════════════════════════════

🎯 EXECUTION CHECKLIST

[ ] Read FORTRESS_QUICK_START.md
[ ] Git commit all current changes
[ ] Run ./scan_sovereign_structure.sh
[ ] Review audit report
[ ] Run ./reorganize_sovereign_fortress.sh
[ ] Share audit with Claude
[ ] Execute migration with Claude's guidance
[ ] Run npm run type-check
[ ] Fix TypeScript errors
[ ] Run npm test
[ ] Run npm run build
[ ] Git commit with fortress architecture
[ ] Celebrate 🏴

═══════════════════════════════════════════════════════════════════

⏱️  ESTIMATED TIME: 90-120 minutes
🛡️  RISK LEVEL: LOW (full backups + git safety)
💰 VALUE: Foundation for million-dollar trading empire

Your move, Commander. 🏴

═══════════════════════════════════════════════════════════════════

